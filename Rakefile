require "rubygems"
require "rake"
require "colorize"

desc "Default task."
task :default do
	puts "Nothing to see here... move along.".green
	puts "Run".yellow + " `bukkit new my-awesome-server-name`"
end
